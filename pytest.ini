[pytest]
testpaths = tests
markers =
    defect: reproduces a logged defect in defect_log.csv; expected to fail
            against the current build. Exclude with: pytest -m "not defect"
    integration: exercises more than one layer (route, business logic, model, DB)
    system: end-to-end Selenium browser test; requires chromedriver on PATH
